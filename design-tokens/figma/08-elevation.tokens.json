{
  "$description": "WEREAL REOS Design Tokens — Box shadows (STRING — bind manually in Figma effects)",
  "shadow": {
    "sm": {
      "$value": "0 1px 2px rgba(0, 0, 0, 0.05)",
      "$type": "string",
      "$description": "CSS box-shadow — apply as effect style or Dev Mode note"
    },
    "md": {
      "$value": "0 4px 12px rgba(0, 0, 0, 0.08)",
      "$type": "string",
      "$description": "CSS box-shadow — apply as effect style or Dev Mode note"
    },
    "lg": {
      "$value": "0 8px 24px rgba(0, 0, 0, 0.12)",
      "$type": "string",
      "$description": "CSS box-shadow — apply as effect style or Dev Mode note"
    }
  }
}
